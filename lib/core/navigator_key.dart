import 'package:flutter/material.dart';

/// Chave global do navigator para push de rotas de qualquer lugar (ex: InstaHub no trigger).
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
